<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Store Items</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: url('https://images8.alphacoders.com/135/1354012.png') no-repeat center center fixed;
            background-size: cover;
            margin: 0;
            padding: 0;
        }

        nav {
            background: rgba(0, 0, 0, 0.7);
            padding: 1rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        nav .logo {
            color: white;
            font-size: 1.5rem;
            text-transform: uppercase;
            font-weight: bold;
        }

        nav ul {
            list-style: none;
            display: flex;
            gap: 1rem;
            margin: 0;
            padding: 0;
        }

        nav ul li {
            display: inline;
        }

        nav ul li a {
            text-decoration: none;
            color: white;
            font-size: 1rem;
            transition: color 0.3s;
        }

        nav ul li a:hover {
            color: #6e8efb;
        }

        .container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            padding: 2rem;
        }

        .card {
            background: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            width: 300px;
            margin: 1rem;
            overflow: hidden;
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .card:hover {
            transform: translateY(-10px);
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.2);
        }

        .card-body {
            padding: 1rem;
            text-align: center;
        }

        .card-title {
            font-size: 1.5rem;
            color: #333;
            margin-bottom: 0.5rem;
        }

        .card-description {
            font-size: 1rem;
            color: #666;
            margin-bottom: 1rem;
        }

        .card-button {
            background: #6e8efb;
            color: white;
            border: none;
            padding: 0.5rem 1rem;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            font-size: 0.9rem;
            transition: background 0.3s;
        }

        .card-button:hover {
            background: #5a7de2;
        }

        .header {
            text-align: center;
            font-size: 2rem;
            margin-top: 2rem;
            margin-bottom: 2rem;
            color: white;
            text-shadow: 0 2px 5px rgba(0, 0, 0, 0.5);
        }
    </style>
</head>
<body>
    <nav>
        <div class="logo">BarangBareng</div>
        <ul>
            <li><a href="/categories">Home</a></li>
            <li><a href="/itemsforsale">See All Items</a></li>
        </ul>
    </nav>

    <div class="header">Barang Barengan</div>
    <div class="container">
        <% stores.forEach((el) => { %>
            <div class="card">
                <div class="card-body">
                    <div class="card-title"><%= el.name %></div>
                    <div class="card-description">Discover various items in <%= el.name %>.</div>
                    <a href="/categories/<%= el.id %>" class="card-button">DETAIL ITEMS</a>
                </div>
            </div>
        <% }) %>
    </div>
</body>
</html>