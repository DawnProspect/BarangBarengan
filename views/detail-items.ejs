<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detail Item</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: url('https://images8.alphacoders.com/135/1354012.png') no-repeat center center fixed;
            background-size: cover;
            margin: 0;
            padding: 0;
            color: #fff;
        }

        nav {
            background: rgba(0, 0, 0, 0.7);
            padding: 1rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        nav .logo {
            color: white;
            font-size: 1.5rem;
            text-transform: uppercase;
            font-weight: bold;
        }

        nav ul {
            list-style: none;
            display: flex;
            gap: 1rem;
            margin: 0;
            padding: 0;
        }

        nav ul li {
            display: inline;
        }

        nav ul li a {
            text-decoration: none;
            color: white;
            font-size: 1rem;
            transition: color 0.3s;
        }

        nav ul li a:hover {
            color: #6e8efb;
        }

        .container {
            display: flex;
            justify-content: center;
            padding: 2rem;
        }

        .card {
            background: rgba(255, 255, 255, 0.9);
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            padding: 2rem;
            width: 100%;
            max-width: 600px;
            text-align: center;
        }

        .card img {
            width: 100%;
            max-width: 300px;
            border-radius: 10px;
            object-fit: cover;
            transition: transform 0.3s ease-in-out;
        }

        .card img:hover {
            transform: scale(1.05);
        }

        .card h1 {
            font-size: 2rem;
            color: #333;
            margin-bottom: 1rem;
        }

        .card p {
            font-size: 1rem;
            color: #666;
            margin-bottom: 1rem;
        }

        .card .price {
            font-weight: bold;
            color: #28a745;
            font-size: 1.5rem;
            margin-bottom: 1rem;
        }

        .card .actions {
            display: flex;
            justify-content: center;
            gap: 1rem;
        }

        .card .button {
            padding: 0.5rem 1rem;
            border-radius: 5px;
            text-decoration: none;
            color: white;
            background-color: #6e8efb;
            font-weight: bold;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .card .button:hover {
            background-color: #5a7de2;
        }

        .footer {
            text-align: center;
            font-size: 1rem;
            color: #666;
            margin-top: 2rem;
        }
    </style>
</head>

<body>
    <nav>
        <div class="logo">BarangBareng</div>
        <ul>
            <li><a href="/categories">Home</a></li>
            <li><a href="#">Stores</a></li>
            <li><a href="#">Contact</a></li>
            <li><a href="/itemsforsale">See All Items</a></li>
        </ul>
    </nav>

    <div class="container">
        <div class="card">
            <img src="<%= stores.imageURL %>" alt="<%= stores.name %>">
            <h1><%= stores.name %></h1>
            <p><%= stores.description %></p>
            <p class="price">$<%= stores.price %></p>
            <p>Stock: <%= stores.stock %> | Condition: <%= stores.condition %></p>
            <p>Created: <%= stores.formattedDate %></p>
            <div class="actions">
                <a href="/itemsforsale/categories/<%= stores.id %>/edit" class="button">Edit</a>
                <a href="/itemsforsale/categories/<%= stores.id %>/delete" class="button">Delete</a>
            </div>
        </div>
    </div>

    <div class="footer">
    </div>
</body>

</html>
