<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Item</title>
    <style>
        /* Resetting default styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f9;
            color: #333;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: url('https://images8.alphacoders.com/135/1354012.png') no-repeat center center fixed;
            background-size: cover;
        }

        .form-container {
            background-color: rgba(255, 255, 255, 0.9);
            border-radius: 10px;
            padding: 2rem;
            width: 100%;
            max-width: 500px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
        }

        h1 {
            text-align: center;
            font-size: 2rem;
            color: #333;
            margin-bottom: 1.5rem;
        }

        label {
            font-size: 1rem;
            color: #333;
            font-weight: bold;
            margin-bottom: 0.5rem;
        }

        input,
        select,
        textarea {
            width: 100%;
            padding: 0.8rem;
            margin: 0.5rem 0;
            border-radius: 5px;
            border: 1px solid #ccc;
            font-size: 1rem;
        }

        input[type="submit"] {
            background-color: #6e8efb;
            color: white;
            font-weight: bold;
            border: none;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        input[type="submit"]:hover {
            background-color: #5a7de2;
        }

        input[type="submit"]:active {
            background-color: #4868c2;
        }

        .footer {
            text-align: center;
            font-size: 1rem;
            color: #666;
            margin-top: 2rem;
        }

        textarea {
            resize: vertical;
            min-height: 150px;
        }
    </style>
</head>

<body>
    <div class="form-container">
        <h1>Edit Item</h1>
        <% if (locals.error) { %>
            <div style="color: red; background: rgba(255, 0, 0, 0.1); padding: 10px; border-radius: 5px; margin: 10px 0;">
                <%= error %>
            </div>
        <% } %>  
        <form action="/itemsforsale/categories/<%= stores.id %>/edit" method="POST">
            <div>
                <label for="name">Product Name:</label>
                <input type="text" name="name" id="name" value="<%= stores.name %>">
            </div>
            <div>
                <label for="stock">Stock Quantity:</label>
                <input type="number" name="stock" id="stock" value="<%= stores.stock %>">
            </div>
            <div>
                <label for="imageURL">Image URL:</label>
                <input type="url" name="imageURL" id="imageURL" value="<%= stores.imageURL %>">
            </div>
            <div>
                <label for="description">Product Description:</label>
                <textarea name="description" id="description"><%= stores.description %></textarea>
            </div>
            <div>
                <label for="condition">Condition:</label>
                <select name="condition" id="condition" required>
                    <option value="like-new" <%= stores.condition === 'like-new' ? 'selected' : '' %>>Like-New</option>
                    <option value="used" <%= stores.condition === 'used' ? 'selected' : '' %>>Used</option>
                </select>
            </div>
            <div>
                <label for="price">Price:</label>
                <input type="number" name="price" id="price" value="<%= stores.price %>">
            </div>
            <input type="submit" value="Save Changes">
        </form>
    </div>

    <div class="footer">
    </div>
</body>

</html>
